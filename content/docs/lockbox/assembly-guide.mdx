---
title: Lockbox Assembly Guide
---

## Purpose

This work instruction documents the current Lockbox assembly process from printed parts through final mechanical assembly.

Use these instructions with the active PCB and CAD release for the build.

## Required parts and consumables

1. Gather the required parts before starting assembly.

    <figure className="photo-frame" style={{ maxWidth: "720px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/assembly-required-parts.png" alt="Required Lockbox assembly parts" style={{ width: "100%", height: "auto" }} />
     <figcaption>Required Lockbox assembly parts.</figcaption>
    </figure>

2. Confirm the assembly set includes:

    - `top-housing`
    - `bottom-housing`
    - `pcb-backing`
    - `locking-latch`
    - `enter-button`
    - `back-button`
    - `encoder-wheel`
    - `encoder`
    - `motor`
    - `screen`
    - `battery`
    - 7x `torx-screw`
    - 4x `4mm-magnet`
    - 4x `rubber-foot`
    - `VHB-double-sided-tape`
    - `hot-glue`
    - `SA-glue`

3. If all required parts are present, continue onward.

## Tools

- Electric screwdriver or hand driver with T7 Torx bit
- `flat-jaw-pliers`
- `tweezers`
- `paperclip`
- `isopropyl-alcohol`
- `microfibre-cloth`

## Prepare plastic components

1. Work on a soft cloth. The front face scratches easily.

2. Check each printed part in good lighting before assembly. Look for curled corners, squished layers, large plastic blobs, blocked magnet inserts, and blocked screw holes.

3. Use a smooth PEI print bed for front-facing cosmetic parts and a textured PEI print bed for internal parts.

4. Inspect each printed part before installing electronics.
5. Confirm the plastic components are clean, correctly printed, and free of obstructions before continuing.

### Part reference photos

Use these photos while inspecting printed parts.

#### `top-housing`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-top-housing-main.png" alt="Top housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Top housing.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-top-housing-side.png" alt="Top housing side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Top housing side view.</figcaption>
     </figure>
   </div>

#### `bottom-housing`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-bottom-housing-main.png" alt="Bottom housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Bottom housing.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-bottom-housing-side.png" alt="Bottom housing side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Bottom housing side view.</figcaption>
     </figure>
   </div>

#### `motor-clamp`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-motor-clamp-main.png" alt="Motor clamp" style={{ width: "100%", height: "auto" }} />
       <figcaption>Motor clamp.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-motor-clamp-side.png" alt="Motor clamp side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Motor clamp side view.</figcaption>
     </figure>
   </div>

#### `pcb-cover`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-pcb-cover-main.png" alt="PCB cover" style={{ width: "100%", height: "auto" }} />
       <figcaption>PCB cover.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-pcb-cover-side.png" alt="PCB cover side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>PCB cover side view.</figcaption>
     </figure>
   </div>

#### `encoder-wheel`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-encoder-wheel-main.png" alt="Encoder wheel" style={{ width: "100%", height: "auto" }} />
       <figcaption>Encoder wheel.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-encoder-wheel-side.png" alt="Encoder wheel side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Encoder wheel side view.</figcaption>
     </figure>
   </div>

#### `locking-latch`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-locking-latch-main.png" alt="Locking latch" style={{ width: "100%", height: "auto" }} />
       <figcaption>Locking latch.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-locking-latch-side.png" alt="Locking latch side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Locking latch side view.</figcaption>
     </figure>
   </div>

#### `enter-button`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-enter-button-main.png" alt="Enter button" style={{ width: "100%", height: "auto" }} />
       <figcaption>Enter button.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-enter-button-side.png" alt="Enter button side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Enter button side view.</figcaption>
     </figure>
   </div>

#### `back-button`

   <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-back-button-main.png" alt="Back button" style={{ width: "100%", height: "auto" }} />
       <figcaption>Back button.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/part-back-button-side.png" alt="Back button side view" style={{ width: "100%", height: "auto" }} />
       <figcaption>Back button side view.</figcaption>
     </figure>
   </div>

## Install magnets

1. Install four magnets: one in the `locking-latch`, one in the `top-housing`, and two in the `bottom-housing`.

2. Use a `paperclip` to lightly coat the inside of each magnet insert with `SA-glue`.

    <figure className="photo-frame" style={{ maxWidth: "520px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/build-magnet-glue-paperclip.jpg" alt="Applying superglue to magnet insert with a paperclip" style={{ width: "100%", height: "auto" }} />
     <figcaption>Apply a light coat of glue inside the magnet insert.</figcaption>
    </figure>

3. Check magnet polarity before pressing each magnet into place:

    - `locking-latch`: north pole up
    - `top-housing` front magnet: south side up
    - `bottom-housing` front magnet: north side up
    - `bottom-housing` back magnet: south side up

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "320px", margin: "0" }}>
       <img src="assets/build-top-housing-magnet-polarity.jpg" alt="Checking front housing magnet polarity" style={{ width: "100%", height: "auto" }} />
       <figcaption>Top housing front magnet polarity.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "320px", margin: "0" }}>
       <img src="assets/build-bottom-housing-front-magnet-polarity.jpg" alt="Checking bottom housing front magnet polarity" style={{ width: "100%", height: "auto" }} />
       <figcaption>Bottom housing front magnet polarity.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "320px", margin: "0" }}>
       <img src="assets/build-bottom-housing-back-magnet-polarity.jpg" alt="Checking bottom housing back magnet polarity" style={{ width: "100%", height: "auto" }} />
       <figcaption>Bottom housing back magnet polarity.</figcaption>
     </figure>
    </div>

4. Use `flat-jaw-pliers` to press magnets into their inserts. Place a folded `microfibre-cloth` between the pliers and the Lockbox body to protect the printed surface.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/build-magnet-press-protection.jpg" alt="Protecting the housing while pressing magnets" style={{ width: "100%", height: "auto" }} />
       <figcaption>Protect the housing while pressing magnets.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/build-locking-latch-magnet-press.jpg" alt="Pressing lock arm magnet with flat jaw pliers" style={{ width: "100%", height: "auto" }} />
       <figcaption>Press the lock arm magnet into place.</figcaption>
     </figure>
    </div>

5. Confirm each magnet is seated flush or deep enough for its insert.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "340px", margin: "0" }}>
       <img src="assets/build-top-housing-magnet-installed.jpg" alt="Magnet installed in top housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Top housing magnet installed.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "340px", margin: "0" }}>
       <img src="assets/build-bottom-housing-back-magnet-installed.jpg" alt="Back magnet installed in bottom housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Bottom housing back magnet installed.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "340px", margin: "0" }}>
       <img src="assets/step-01-magnet-locking-latch.png" alt="Magnet installed in locking latch" style={{ width: "100%", height: "auto" }} />
       <figcaption>Locking latch magnet installed.</figcaption>
     </figure>
    </div>

6. Confirm the magnets are fully seated and glued before continuing.

## Install screen and battery

1. Remove the protective film from the `screen`.

2. Align the `screen` with the top and right ledges of the `top-housing`.

3. Hold the screen in place and set it with `hot-glue` at the corners and remaining edges.

4. Wipe the rear of the `screen` and the side of the `battery` with writing using `isopropyl-alcohol`.

5. Apply a 3/4 inch square piece of `VHB-double-sided-tape` to the writing side of the `battery`.

    <figure className="photo-frame" style={{ maxWidth: "520px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/build-battery-vhb-tape.jpg" alt="VHB tape applied to battery" style={{ width: "100%", height: "auto" }} />
     <figcaption>VHB tape applied to the battery.</figcaption>
    </figure>

6. Place the `battery` face down on the back of the `screen` with the cables exiting toward the lower right. Set with `hot-glue` as needed.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/build-screen-battery-layout.jpg" alt="Screen and battery installed in top housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Screen and battery installed in the top housing.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/build-screen-motor-layout.jpg" alt="Screen, battery, and motor layout reference" style={{ width: "100%", height: "auto" }} />
       <figcaption>Screen, battery, and motor layout reference.</figcaption>
     </figure>
    </div>

7. Confirm the screen and battery are secure and that wires remain accessible.

## Install encoder

1. Test-fit the `encoder-wheel` before soldering or gluing.

2. Apply `SA-glue` to the flat face of the encoder shaft.

3. Line up the flat of the encoder shaft with the flat face inside the `encoder-wheel`, then install the wheel.

4. Solder the `encoder` onto the PCB. Make sure the encoder is fully seated so the wheel stays straight.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/step-03-encoder-on-pcb.png" alt="Encoder installed on PCB" style={{ width: "100%", height: "auto" }} />
       <figcaption>Encoder installed on PCB.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/step-03-encoder-wheel-installed.png" alt="Encoder wheel installed" style={{ width: "100%", height: "auto" }} />
       <figcaption>Encoder wheel installed.</figcaption>
     </figure>
    </div>

5. Confirm the encoder wheel rotates freely before continuing.

## Install motor

1. Check motor polarity before installing the motor assembly.

2. If soldering the motor lead during this build, cut the motor connector lead length to 7 cm before final routing.

3. Place the `locking-latch` on the `motor`. Do not glue the latch to the motor.

    <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/step-04-latch-on-motor.png" alt="Locking latch placed on motor" style={{ width: "100%", height: "auto" }} />
     <figcaption>Locking latch placed on the motor.</figcaption>
    </figure>

4. Slide the latch onto the motor assembly about halfway, then place the `motor` in the `top-housing`.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/build-motor-latch-halfway.jpg" alt="Locking latch installed halfway on motor" style={{ width: "100%", height: "auto" }} />
       <figcaption>Latch installed about halfway on motor.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "360px", margin: "0" }}>
       <img src="assets/step-04-motor-in-housing.png" alt="Motor placed in housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Motor placed in housing.</figcaption>
     </figure>
    </div>

5. Place the `motor-clamp` over the motor and secure it with 2x `torx-screw`.

    <figure className="photo-frame" style={{ maxWidth: "520px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/build-motor-clamp-installed.jpg" alt="Motor clamp installed" style={{ width: "100%", height: "auto" }} />
     <figcaption>Motor clamp installed.</figcaption>
    </figure>

6. Confirm the motor is seated and the latch can move without obstruction.

## Install buttons and PCB

1. Place the `enter-button` and `back-button` into the button holes.

    <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/step-05-buttons-in-housing.png" alt="Buttons placed in housing" style={{ width: "100%", height: "auto" }} />
     <figcaption>Buttons placed in housing.</figcaption>
    </figure>

2. Place the `back-button` on the left and the lock/enter button on the right.

3. If the buttons are not already populated, solder the buttons onto the bottom of the PCB, the side with no other components.

4. Place the PCB onto the buttons so the `encoder-wheel` passes through the front hole.

    <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/step-05-pcb-placement.png" alt="PCB placement over buttons" style={{ width: "100%", height: "auto" }} />
     <figcaption>PCB placement over buttons.</figcaption>
    </figure>

5. Do not cover the `screen-ribbon-wire`, `battery-wire`, or `motor-wire`.

    <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/step-05-pcb-seated.png" alt="PCB seated in housing" style={{ width: "100%", height: "auto" }} />
     <figcaption>PCB seated in housing.</figcaption>
    </figure>

6. Confirm the buttons actuate correctly and the encoder wheel is centered in the opening.

## Connect cables

1. Open the screen ribbon connector latch.

2. Insert the `screen-ribbon-wire`, then close the latch. The lower white bar should not be visible after the latch is closed.

3. Plug the `battery-wire` and `motor-wire` into the PCB.

4. Route the motor wires left and over the PCB.

5. Keep the battery cable away from the mounting hole below it so the cable does not get pinched by the cover.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/step-06-cables-connected.png" alt="Screen, battery, and motor cables connected" style={{ width: "100%", height: "auto" }} />
       <figcaption>Screen, battery, and motor cables connected.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/build-pcb-cable-routing.jpg" alt="PCB cable routing reference" style={{ width: "100%", height: "auto" }} />
       <figcaption>PCB cable routing reference.</figcaption>
     </figure>
    </div>

6. Confirm each cable is fully seated before continuing.

## Install PCB cover

1. Place the `pcb-cover` over the PCB.

    <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/step-07-pcb-cover-placement.png" alt="PCB cover placement" style={{ width: "100%", height: "auto" }} />
     <figcaption>PCB cover placement.</figcaption>
    </figure>

2. Secure the cover with 5x `torx-screw`.

    <figure className="photo-frame" style={{ maxWidth: "520px", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <img src="assets/build-pcb-cover-installed.jpg" alt="PCB cover installed" style={{ width: "100%", height: "auto" }} />
     <figcaption>PCB cover installed.</figcaption>
    </figure>

3. Confirm no wires are pinched under the cover.

## Install rubber feet

1. Glue 4x `rubber-foot` onto the backplate.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "320px", margin: "0" }}>
       <img src="assets/step-08-rubber-feet-closeup.png" alt="Rubber feet closeup" style={{ width: "100%", height: "auto" }} />
       <figcaption>Rubber foot closeup.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/build-backplate-rubber-feet-installed.jpg" alt="Rubber feet installed on backplate" style={{ width: "100%", height: "auto" }} />
       <figcaption>Rubber feet installed on backplate.</figcaption>
     </figure>
    </div>

2. Confirm all four feet are present and secure.

## Final assembly

1. Slide the backplate onto the main housing.

    <div className="photo-row" style={{ display: "flex", flexWrap: "wrap", gap: "1rem", margin: "0.75rem 0 1.5rem 1.5rem" }}>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/step-09-backplate-slide.png" alt="Backplate sliding onto housing" style={{ width: "100%", height: "auto" }} />
       <figcaption>Backplate sliding onto housing.</figcaption>
     </figure>
     <figure className="photo-frame" style={{ maxWidth: "420px", margin: "0" }}>
       <img src="assets/step-09-final-assembly.png" alt="Final Lockbox assembly" style={{ width: "100%", height: "auto" }} />
       <figcaption>Final Lockbox assembly.</figcaption>
     </figure>
    </div>

2. Assembly is complete. Continue to functional testing.

## QC checklist

- Confirm all required parts were present before assembly.
- Confirm printed parts are free of curled corners, squished layers, large blobs, blocked magnet inserts, and blocked screw holes.
- Confirm magnets are installed in the `top-housing`, `bottom-housing`, and `locking-latch`.
- Confirm all 4 magnets are installed with the documented polarity.
- Confirm the `screen` and `battery` are secured with `hot-glue`.
- Confirm the `screen-ribbon-wire` is latched and the lower white bar is not visible.
- Confirm the `encoder` is soldered and the `encoder-wheel` rotates freely.
- Confirm the `locking-latch` is placed on the `motor` without glue.
- Confirm motor polarity and, if built by hand, the 7 cm motor connector lead length.
- Confirm the `motor-clamp` is secured with 2x `torx-screw`.
- Confirm the motor clamp is straight.
- Confirm the PCB is seated without covering the `screen-ribbon-wire`, `battery-wire`, or `motor-wire`.
- Confirm the `screen-ribbon-wire`, `battery-wire`, and `motor-wire` are connected.
- Confirm the buttons click cleanly.
- Confirm the encoder wheel is aligned in the front cutout and spins freely.
- Confirm the `pcb-cover` is secured with 5x `torx-screw`.
- Confirm 4x `rubber-foot` are installed on the backplate.
- Confirm the backplate slides onto the main housing.
